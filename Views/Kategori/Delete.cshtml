@model Kategori
@{
    Layout = "_AdminLayout";
}
<div class="row">
    <div class="col">
        <div class="card">
            <div class="card-body text-center">
                <p>@Model.KategoriAdi isimli kategoriyi silmek istiyor musunuz?</p>
                <form method="post" asp-controller="Kategori" asp-action="DeleteConfirm">
                    <input type="hidden" name="Id" value="@Model.Id">
                    <button type="submit" class="btn btn-danger">Evet</button>
                    <a asp-controller="Kategori" asp-action="Index" class="btn btn-secondary">İptal</a>
                </form>
            </div>
        </div>
    </div>
</div>